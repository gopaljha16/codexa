FROM node:18-bullseye

# WORKDIR /app

# # Install build dependencies required for bcrypt and other native modules
# RUN apt-get update && apt-get install -y \
#     python3 \
#     build-essential \
#     && rm -rf /var/lib/apt/lists/*

# COPY package*.json ./

# # Install dependencies - npm will rebuild native modules for Linux
# RUN npm ci

# COPY . .

# EXPOSE 3000
# CMD ["npm", "start"]
